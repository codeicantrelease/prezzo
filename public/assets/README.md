# Assets

Put local videos, GIFs, screenshots, generated images, audio, and small demo files here.

For each non-trivial asset, record:

- source URL or generation prompt
- license/usage rights
- date created or downloaded
- whether the asset may be committed

Large render outputs belong in `out/`, not here.
